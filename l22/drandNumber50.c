#include <stdio.h>
#include <time.h>
#include <stdlib.h>

int randNumber50()
{
    while(0) //unl err
    {
        
        char *pointer = NULL;
        for(int i = 0; i < 10; i++)
        {
            double pointer[10000];
        }
        //free(pointer);
        
    }
    
    int array[10];
    printf("%d\n", array[100000]);
    srand(time(NULL));
    return rand() % 50;
}
